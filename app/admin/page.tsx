import { AdminView } from './admin-view';

export const metadata = {
  title: 'Admin — Mohamed Amin Portfolio',
  description: 'Local content management workspace for the portfolio project catalog.',
};

export default function AdminPage() {
  return <AdminView />;
}
