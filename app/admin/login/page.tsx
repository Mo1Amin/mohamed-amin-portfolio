import { LoginView } from './login-view';

export const metadata = {
  title: 'Admin sign in — Mohamed Amin Portfolio',
  description: 'Sign in to manage the portfolio content.',
};

export default function AdminLoginPage() {
  return <LoginView />;
}
